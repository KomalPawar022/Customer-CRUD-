CRUD application for Customer


Create, view, update, and delete customer information.
Authentication: implements JWT authentication.

Instructions:-
1) Create a Database Customers.
2) Create a table Customer :- 
CREATE TABLE Customer (
  uuid VARCHAR(255) PRIMARY KEY,
  first_name VARCHAR(50),
  last_name VARCHAR(50),
  street VARCHAR(50),
  address VARCHAR(255),
  city VARCHAR(50),
  state VARCHAR(50),
  email VARCHAR(50),
  phone VARCHAR(50)
);
3) Change user and password in DBConnection.java in com.connection.
